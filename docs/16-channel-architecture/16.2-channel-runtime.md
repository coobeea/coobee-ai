# 16.2 - ChannelRuntime 核心调度层

> **任务 ID**: Task-2  
> **优先级**: P0  
> **依赖**: Task-1（ChannelPlugin 接口定义）  
> **预计工期**: 0.5 天

---

## 1. 需求描述

### 1.1 用户需求

**背景**：

- 当前每个 Extension（酒馆、共享网盘）都要手动加载 AgentStore、构建 Builder、注入工具/技能
- 代码重复度高，逻辑分散在各个 Extension 中
- 缺乏统一的 Agent 调度层，无法实现统一的 Session 管理、Context 注入、错误处理

**核心需求**：

1. 提供统一的 Agent 执行接口（所有 Channel 都调这个接口）
2. 自动加载 Agent 定义、构建 Builder、注入工具/技能
3. 支持 Context 增强（根据 Channel 类型注入不同上下文）
4. 支持独立 Session 管理（每个 Channel + Agent 组合独立 Session）

**期望效果**：

- Channel 只需调用一个方法：`runtime.executeAgent()`
- 所有复杂逻辑（加载、构建、注入）由 Runtime 处理
- Context 自动增强（Discussion 注入讨论上下文，Feishu 注入飞书上下文）

---

### 1.2 技术约束

1. **复用现有能力**：
   - 使用现有的 `agentExecutor`（不重新实现执行器）
   - 使用现有的 `AgentStore`（加载 Agent 定义）
   - 使用现有的 `ToolRegistry` / `SkillManager`（工具/技能管理）

2. **单例模式**：
   - ChannelRuntime 必须是单例
   - 全局唯一实例，避免重复初始化

3. **TypeScript 类型安全**：
   - 所有方法必须有完整的类型定义
   - 返回值必须明确（避免 `void` / `unknown`）

---

## 2. 解决方案

### 2.1 设计思路

**核心理念**：ChannelRuntime 是"Agent 执行器的统一门面"

```
Channel                  ChannelRuntime                AgentExecutor
  ↓                           ↓                              ↓
收到消息  →  runtime.executeAgent()  →  加载 Agent 定义  →  执行
            (统一入口)                  构建 Builder
                                       注入工具/技能
                                       增强 Context
                                       管理 Session
```

**职责分离**：

| 组件               | 职责       | 示例                             |
| ------------------ | ---------- | -------------------------------- |
| **Channel**        | 消息转换   | 飞书事件 → InboundMessage        |
| **ChannelRuntime** | Agent 调度 | 加载定义、构建 Builder、注入工具 |
| **AgentExecutor**  | Agent 执行 | 调用 LLM、工具调用、流式输出     |

---

### 2.2 核心方法

```typescript
class ChannelRuntime {
  // 执行 Agent（同步模式）
  async executeAgent(params): Promise<{ output: string }>;

  // 流式执行 Agent（异步生成器）
  async *streamAgent(params): AsyncGenerator<StreamChunk, void, unknown>;

  // 增强指令（注入 Channel 上下文）
  private enhanceInstructions(baseInstructions, context): string;
}
```

---

## 3. 详细设计

### 3.1 类定义

```typescript
// src/main/channels/ChannelRuntime.ts

import { agentExecutor } from '@main/ai/AgentExecutor';
import { AgentStore } from '@main/ai/agents/AgentStore';
import { ToolRegistry } from '@main/ai/tools/registry';
import { SkillManager } from '@main/ai/skills';
import { Env } from '@main/common/env';
import { builtinTools } from '@main/ai/tools';
import type { ChannelContext } from './types';
import type { StreamChunk } from '@shared/stream-protocol';

/**
 * ChannelRuntime - 统一的 Agent 调度层
 *
 * 所有 Channel 都通过这个层调用 Agent，确保：
 * 1. 一致的 Session 管理
 * 2. Context 注入
 * 3. 工具/技能加载
 * 4. 错误处理
 */
export class ChannelRuntime {
  private static instance: ChannelRuntime;

  private constructor() {
    // Singleton
  }

  /**
   * 获取单例实例
   */
  static getInstance(): ChannelRuntime {
    if (!ChannelRuntime.instance) {
      ChannelRuntime.instance = new ChannelRuntime();
    }
    return ChannelRuntime.instance;
  }

  /**
   * 执行 Agent（同步模式）
   *
   * @param params - 执行参数
   * @returns 执行结果
   */
  async executeAgent(params: {
    /** Agent ID */
    agentId: string;
    /** Session ID（用于历史管理） */
    sessionId: string;
    /** 用户消息 */
    message: string;
    /** Channel 上下文 */
    context: ChannelContext;
  }): Promise<{ output: string; error?: string }> {
    try {
      // 1. 加载 Agent 定义
      const store = await AgentStore.getInstance();
      const agentDef = await store.get(params.agentId);

      if (!agentDef) {
        return {
          output: '',
          error: `Agent "${params.agentId}" not found`
        };
      }

      // 2. 构建 Builder
      const builder = await this.buildAgentExecutor(agentDef, params.context);

      // 3. 执行（收集完整输出）
      const gen = agentExecutor.stream({
        sessionId: params.sessionId,
        message: params.message,
        builder
      });

      let output = '';
      for await (const chunk of gen) {
        if (chunk.type === 'text:delta') {
          output += chunk.content;
        }
      }

      return { output: output.trim() };
    } catch (err) {
      return {
        output: '',
        error: err instanceof Error ? err.message : String(err)
      };
    }
  }

  /**
   * 流式执行 Agent
   *
   * @param params - 执行参数
   * @yields StreamChunk - 流式输出块
   */
  async *streamAgent(params: {
    agentId: string;
    sessionId: string;
    message: string;
    context: ChannelContext;
  }): AsyncGenerator<StreamChunk, void, unknown> {
    try {
      // 1. 加载 Agent 定义
      const store = await AgentStore.getInstance();
      const agentDef = await store.get(params.agentId);

      if (!agentDef) {
        yield {
          type: 'error',
          content: `Agent "${params.agentId}" not found`,
          timestamp: Date.now()
        };
        return;
      }

      // 2. 构建 Builder
      const builder = await this.buildAgentExecutor(agentDef, params.context);

      // 3. 流式执行
      const gen = agentExecutor.stream({
        sessionId: params.sessionId,
        message: params.message,
        builder
      });

      for await (const chunk of gen) {
        yield chunk;
      }
    } catch (err) {
      yield {
        type: 'error',
        content: err instanceof Error ? err.message : String(err),
        timestamp: Date.now()
      };
    }
  }

  /**
   * 构建 AgentExecutor Builder
   *
   * @param agentDef - Agent 定义
   * @param context - Channel 上下文
   * @returns Builder 实例
   */
  private async buildAgentExecutor(
    agentDef: {
      id: string;
      name: string;
      instructions: string;
      tools?: string[];
      skills?: string[];
      model?: string;
      thinkingLevel?: string;
    },
    context: ChannelContext
  ) {
    // 1. 创建 Builder
    const builder = agentExecutor
      .piMono()
      .name(agentDef.name || agentDef.id)
      .mode('chat')
      .sessionMode('memory')
      .instructions(this.enhanceInstructions(agentDef.instructions, context));

    // 2. 注入工具
    if (agentDef.tools?.length) {
      const registry = ToolRegistry.getInstance();
      const extTools = registry.getAll();
      const allTools = [...builtinTools, ...extTools];
      const selectedTools = allTools.filter((t) => agentDef.tools!.includes(t.name));
      builder.tools(selectedTools);
    }

    // 3. 注入技能
    if (agentDef.skills?.length) {
      const skillManager = new SkillManager();
      skillManager.scanSkills(undefined, Env.paths.secretsDir);
      const skillDefs = agentDef.skills.map((name) => skillManager.getByName(name)).filter((s) => s !== null);
      if (skillDefs.length > 0) {
        builder.skills(skillDefs);
      }
    }

    // 4. 设置模型
    if (agentDef.model) {
      builder.model(agentDef.model);
    }

    // 5. 设置思考层级
    if (agentDef.thinkingLevel) {
      builder.thinkingLevel(agentDef.thinkingLevel);
    }

    return builder;
  }

  /**
   * 增强指令（注入 Channel 上下文）
   *
   * @param baseInstructions - 基础指令
   * @param context - Channel 上下文
   * @returns 增强后的指令
   */
  private enhanceInstructions(baseInstructions: string, context: ChannelContext): string {
    let enhanced = baseInstructions;

    // 根据 Channel 类型注入不同上下文
    switch (context.channel) {
      case 'discussion':
        enhanced += this.buildDiscussionContext(context);
        break;
      case 'feishu':
        enhanced += this.buildFeishuContext(context);
        break;
      case 'slack':
        enhanced += this.buildSlackContext(context);
        break;
      default:
        // 通用上下文
        enhanced += `\n\n## Channel Context\n`;
        enhanced += `- Channel: ${context.channel}\n`;
    }

    return enhanced;
  }

  /**
   * 构建讨论室上下文
   */
  private buildDiscussionContext(context: ChannelContext): string {
    let ctx = `\n\n## Discussion Context\n`;
    ctx += `- Your role: ${context.role || 'Participant'}\n`;
    ctx += `- Topic: ${context.topic || 'General Discussion'}\n`;

    if (context.recentMessages && Array.isArray(context.recentMessages)) {
      ctx += `- Recent messages:\n`;
      const messages = context.recentMessages as Array<{ sender: string; content: string }>;
      messages.forEach((m) => {
        ctx += `  - ${m.sender}: ${m.content}\n`;
      });
    }

    return ctx;
  }

  /**
   * 构建飞书上下文
   */
  private buildFeishuContext(context: ChannelContext): string {
    let ctx = `\n\n## Feishu Context\n`;
    ctx += `- Platform: Feishu (飞书)\n`;
    ctx += `- Chat Type: ${context.chatType || 'unknown'}\n`;
    ctx += `- Chat ID: ${context.chatId || 'unknown'}\n`;

    if (context.mentionedBot) {
      ctx += `- You were @mentioned by the user\n`;
    }

    return ctx;
  }

  /**
   * 构建 Slack 上下文
   */
  private buildSlackContext(context: ChannelContext): string {
    let ctx = `\n\n## Slack Context\n`;
    ctx += `- Workspace: ${context.workspace || 'unknown'}\n`;
    ctx += `- Channel: ${context.channelName || 'unknown'}\n`;

    if (context.threadTs) {
      ctx += `- Thread: ${context.threadTs}\n`;
    }

    return ctx;
  }
}
```

---

### 3.2 使用示例

#### 示例 1: Discussion Channel 调用

```typescript
// extensions/discussion-channel/index.ts

import { ChannelRuntime } from '@main/channels/ChannelRuntime';

const discussionChannel: ChannelPlugin = {
  id: 'discussion',
  name: 'Discussion Room',

  inbound: {
    handleMessage: async (msg: InboundMessage) => {
      const runtime = ChannelRuntime.getInstance();

      // ✅ 一行代码执行 Agent
      const result = await runtime.executeAgent({
        agentId: msg.from,
        sessionId: `discussion-${msg.peer}-${msg.from}`,
        message: msg.text,
        context: msg.context
      });

      if (result.error) {
        console.error('[DiscussionChannel] Error:', result.error);
        return;
      }

      // 发送回复
      await this.outbound!.sendMessage({
        to: msg.peer,
        agentId: msg.from,
        text: result.output
      });
    }
  }
};
```

---

#### 示例 2: Feishu Channel 调用（流式）

```typescript
// extensions/feishu-channel/index.ts

import { ChannelRuntime } from '@main/channels/ChannelRuntime';

const feishuChannel: ChannelPlugin = {
  id: 'feishu',
  name: 'Feishu',

  inbound: {
    handleMessage: async (msg: InboundMessage) => {
      const runtime = ChannelRuntime.getInstance();

      // ✅ 流式执行 Agent
      const stream = runtime.streamAgent({
        agentId: await selectAgent(msg.peer),
        sessionId: `feishu-${msg.peer}`,
        message: msg.text,
        context: msg.context
      });

      // 创建流式卡片
      const card = await createStreamingCard({ to: msg.peer });

      // 实时更新
      for await (const chunk of stream) {
        if (chunk.type === 'text:delta') {
          await card.update(chunk.content);
        }
      }

      // 完成
      await card.finalize();
    }
  }
};
```

---

### 3.3 Context 增强示例

#### 讨论室 Context 增强

**输入**：

```typescript
{
  agentId: 'expert-1',
  sessionId: 'discussion-room-123-expert-1',
  message: '请分析当前讨论的核心问题',
  context: {
    channel: 'discussion',
    roomId: 'room-123',
    role: 'Technical Expert',
    topic: 'Microservices Architecture',
    recentMessages: [
      { sender: 'PM', content: '我们需要拆分单体应用' },
      { sender: 'Dev', content: '建议使用 K8s 部署' }
    ]
  }
}
```

**增强后的指令**：

```
[原始指令]
You are a Technical Expert in our discussion room.

## Discussion Context
- Your role: Technical Expert
- Topic: Microservices Architecture
- Recent messages:
  - PM: 我们需要拆分单体应用
  - Dev: 建议使用 K8s 部署
```

---

#### 飞书 Context 增强

**输入**：

```typescript
{
  agentId: 'customer-support',
  sessionId: 'feishu-oc_abc123',
  message: '如何重置密码？',
  context: {
    channel: 'feishu',
    chatId: 'oc_abc123',
    chatType: 'p2p',
    mentionedBot: true
  }
}
```

**增强后的指令**：

```
[原始指令]
You are a customer support agent.

## Feishu Context
- Platform: Feishu (飞书)
- Chat Type: p2p
- Chat ID: oc_abc123
- You were @mentioned by the user
```

---

### 3.4 错误处理

```typescript
// 执行失败时返回错误
const result = await runtime.executeAgent({ ... });

if (result.error) {
  // 错误类型
  switch (result.error) {
    case 'Agent "xxx" not found':
      // Agent 不存在
      break;
    case 'Model API rate limit exceeded':
      // API 限流
      break;
    default:
      // 通用错误
  }
}
```

---

### 3.5 性能优化

#### 缓存 Agent 定义

```typescript
class ChannelRuntime {
  private agentCache = new Map<string, AgentDefinition>();

  private async getAgent(agentId: string) {
    if (this.agentCache.has(agentId)) {
      return this.agentCache.get(agentId)!;
    }

    const store = await AgentStore.getInstance();
    const agentDef = await store.get(agentId);

    if (agentDef) {
      this.agentCache.set(agentId, agentDef);
    }

    return agentDef;
  }
}
```

#### 缓存 ToolRegistry

```typescript
class ChannelRuntime {
  private toolRegistry?: ToolRegistry;

  private getToolRegistry() {
    if (!this.toolRegistry) {
      this.toolRegistry = ToolRegistry.getInstance();
    }
    return this.toolRegistry;
  }
}
```

---

### 3.6 单元测试

```typescript
// src/main/channels/__tests__/ChannelRuntime.test.ts

import { describe, it, expect, beforeEach } from 'vitest';
import { ChannelRuntime } from '../ChannelRuntime';

describe('ChannelRuntime', () => {
  let runtime: ChannelRuntime;

  beforeEach(() => {
    runtime = ChannelRuntime.getInstance();
  });

  it('should execute agent successfully', async () => {
    const result = await runtime.executeAgent({
      agentId: 'test-agent',
      sessionId: 'test-session',
      message: 'Hello',
      context: { channel: 'test' }
    });

    expect(result.output).toBeTruthy();
    expect(result.error).toBeUndefined();
  });

  it('should return error if agent not found', async () => {
    const result = await runtime.executeAgent({
      agentId: 'non-existent',
      sessionId: 'test-session',
      message: 'Hello',
      context: { channel: 'test' }
    });

    expect(result.output).toBe('');
    expect(result.error).toContain('not found');
  });

  it('should enhance instructions with discussion context', async () => {
    const result = await runtime.executeAgent({
      agentId: 'test-agent',
      sessionId: 'test-session',
      message: 'Hello',
      context: {
        channel: 'discussion',
        role: 'Expert',
        topic: 'AI',
        recentMessages: []
      }
    });

    // 验证 Context 被正确注入
    expect(result.output).toBeTruthy();
  });
});
```

---

## 4. 实施检查清单

### 4.1 开发任务

- [ ] 创建 `src/main/channels/ChannelRuntime.ts` 文件
- [ ] 实现 `executeAgent()` 方法
- [ ] 实现 `streamAgent()` 方法
- [ ] 实现 `buildAgentExecutor()` 方法
- [ ] 实现 `enhanceInstructions()` 及各 Context 构建方法
- [ ] 添加错误处理
- [ ] 添加性能优化（缓存）

### 4.2 测试任务

- [ ] 编写单元测试（`ChannelRuntime.test.ts`）
- [ ] 测试 Agent 执行成功场景
- [ ] 测试 Agent 不存在场景
- [ ] 测试 Context 增强逻辑
- [ ] 测试流式执行

### 4.3 集成任务

- [ ] 在 Discussion Channel 中集成 ChannelRuntime
- [ ] 验证多 Agent 轮转正常工作
- [ ] 验证 Context 注入生效

---

## 5. 验收标准

### 5.1 功能验收

- ✅ `executeAgent()` 正常执行，返回正确结果
- ✅ `streamAgent()` 流式输出正常
- ✅ Context 增强逻辑生效（Discussion/Feishu/Slack）
- ✅ 错误处理健壮（Agent 不存在、API 限流等）

### 5.2 性能验收

- ✅ Agent 定义缓存生效（第二次调用无 DB 查询）
- ✅ 工具/技能加载缓存生效
- ✅ 单次执行耗时 < 5s（不含 LLM API 时间）

### 5.3 代码质量验收

- ✅ 代码通过 ESLint 检查
- ✅ 代码通过 TypeScript 类型检查
- ✅ 单元测试覆盖率 > 80%

---

## 6. 风险与应对

| 风险                       | 影响 | 概率 | 应对措施                    |
| -------------------------- | ---- | ---- | --------------------------- |
| AgentExecutor API 变更     | 高   | 低   | 紧密依赖现有 API，暂不重构  |
| Context 注入逻辑复杂       | 中   | 中   | 分 Channel 类型分别处理     |
| 性能瓶颈（频繁加载 Agent） | 中   | 中   | 添加缓存层                  |
| 单例模式导致测试困难       | 低   | 低   | 提供 `reset()` 方法清理缓存 |

---

## 7. 后续任务

完成本任务后，可以继续：

- **Task-3**: ExtensionApi 扩展（并行任务）
- **Task-5**: Discussion Channel 迁移（依赖 Task-2）

---

## 8. 参考资料

- [OpenClaw ChannelRuntime 实现](https://github.com/openclaw/openclaw)（如有）
- `src/main/ai/AgentExecutor.ts` - 现有执行器
- `extensions/tavern-integration/index.ts` - 现有 Extension 调用方式
