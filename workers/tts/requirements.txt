# TTS Worker 依赖
# 由 RuntimeManager 通过 uv pip install 安装到就地 venv

# HTTP + WebSocket 服务
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
websockets>=14.0

# Edge TTS（微软免费在线语音合成，无需 API Key）
edge-tts>=6.1.0

# TTS 模型（Qwen3-TTS，本地模式使用）
torch>=2.0.0
torchaudio>=2.0.0
transformers>=4.57.3
modelscope>=1.9.0
qwen-tts>=0.1.0
soundfile>=0.12.0
numpy>=1.21.0
